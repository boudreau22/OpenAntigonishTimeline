# Source Code

This directory contains the source code for the Antigonish Capital Projects Timeline.

- `data/`: Contains the project data (`town_projects.json`).
- `components/`: React components (future migration).
- `scripts/`: JavaScript logic (future migration).
- `styles/`: CSS styles (future migration).
